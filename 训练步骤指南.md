# 🚀 训练模型完整步骤指南

## 📋 训练前准备清单

### ✅ 第一步：确认环境
```bash
# 1. 激活 Anaconda 环境（推荐）
conda activate AiDiagnosis-3.12

# 或激活 venv 环境（已弃用）
# AiDiagnosis_env\Scripts\activate  # Windows
# source AiDiagnosis_env/bin/activate  # macOS/Linux

# 2. 确认已安装必要依赖（应该已经安装了）
pip list | findstr "torch yolov5"
```

### ✅ 第二步：准备数据

你需要准备两类数据：

#### 1️⃣ YOLOv5检测数据（用于检测舌头位置）
- **需要做什么**：标注舌头在图片中的位置（画边界框）
- **需要多少数据**：至少200-300张，推荐500-1000张
- **数据格式**：原始图片 + 标注文件（.txt格式）

#### 2️⃣ ResNet分类数据（用于分类舌象特征）
- **需要做什么**：将舌头图片分类到不同类别文件夹
- **需要多少数据**：每个类别至少50-100张，推荐200-500张
- **分类任务**：
  - 舌色（5类）：淡红、红、绛、淡白、青紫
  - 苔色（3类）：白、黄、灰黑
  - 厚度（2类）：薄、厚
  - 腐腻（2类）：正常、腐腻

---

## 🎯 开始训练（按顺序执行）

### 步骤1：创建数据集目录结构

```bash
# 创建ResNet分类数据的目录结构
python setup_dataset_structure.py --path ./data
```

这会自动创建所有需要的文件夹。

### 步骤2：准备和标注数据

#### 2.1 准备YOLOv5检测数据

**选项A：如果你还没有标注数据**
1. ✅ 标注工具已安装（labelImg 1.8.6，已修复所有错误）
2. ✅ 数据目录已创建：
   ```bash
   mkdir yolo_data\images\train
   mkdir yolo_data\images\val
   mkdir yolo_data\labels\train
   mkdir yolo_data\labels\val
   ```
3. 将你的舌象图片放入 `yolo_data\images\train` 和 `yolo_data\images\val`
4. 启动LabelImg标注：
   ```bash
   # 使用批处理脚本（推荐）
   启动标注工具.bat
   
   # 或直接运行
   conda activate AiDiagnosis-3.12
   labelimg
   ```
5. 在LabelImg中：
   - 打开目录：`yolo_data\images\train`
   - 设置保存目录：`yolo_data\labels\train`
   - 选择YOLO格式（不是PascalVOC）
   - 使用 `W` 键创建边界框，标注舌头位置
   - 类别名称：`tongue`

**选项B：如果你已经有标注好的数据**
- 确保数据格式正确：
  ```
  yolo_data/
  ├── images/
  │   ├── train/    # 训练图片
  │   └── val/      # 验证图片
  └── labels/
      ├── train/    # 训练标注（.txt文件）
      └── val/      # 验证标注（.txt文件）
  ```

#### 2.2 准备ResNet分类数据

1. 将已裁剪的舌头图片（或从原图中裁剪的舌头区域）分类放入对应文件夹：
   ```
   data/
   ├── tongue_color/
   │   ├── train/
   │   │   ├── class0/  # 淡红舌
   │   │   ├── class1/  # 红舌
   │   │   ├── class2/  # 绛舌
   │   │   ├── class3/  # 淡白舌
   │   │   └── class4/  # 青紫舌
   │   └── val/        # 验证集（同样结构）
   ├── tongue_coat_color/
   │   ├── train/
   │   │   ├── class0/  # 白苔
   │   │   ├── class1/  # 黄苔
   │   │   └── class2/  # 灰黑苔
   │   └── val/
   ├── thickness/
   │   ├── train/
   │   │   ├── class0/  # 薄
   │   │   └── class1/  # 厚
   │   └── val/
   └── rot_and_greasy/
       ├── train/
       │   ├── class0/  # 正常
       │   └── class1/  # 腐腻
       └── val/
   ```

2. **提示**：如果图片还没裁剪，可以先用YOLOv5检测模型检测并裁剪舌头区域。

### 步骤3：检查数据格式

```bash
# 检查YOLOv5数据（需要先创建配置文件）
copy data\tongue.yaml.example data\tongue.yaml
# 编辑 data\tongue.yaml，修改path为你的实际路径
python check_dataset.py --yolo_data data\tongue.yaml

# 检查ResNet数据
python check_dataset.py --resnet_data ./data
```

### 步骤4：训练YOLOv5检测模型

```bash
# 1. 创建配置文件（如果还没创建）
copy data\tongue.yaml.example data\tongue.yaml

# 2. 编辑 data\tongue.yaml，修改path路径为你的实际数据路径
# 例如：path: D:\MyData\yolo_data

# 3. 开始训练
# 使用批处理脚本（推荐）
训练YOLOv5模型.bat

# 或手动运行
conda activate AiDiagnosis-3.12
python train_yolov5.py --data data\tongue.yaml --epochs 100 --batch 16

# 参数说明：
# --data: 数据集配置文件路径
# --epochs: 训练轮数（建议100-200）
# --batch: 批次大小（根据GPU内存调整，16-32）
```

**训练完成后**：
- 最佳模型保存在：`runs\train\tongue_detection\weights\best.pt`
- 训练日志和图表在：`runs\train\tongue_detection\`

### 步骤5：训练ResNet分类模型

你可以选择**单个训练**或**批量训练**：

#### 选项A：单个训练（推荐，可以观察每个模型的训练过程）

```bash
# 使用批处理脚本（推荐，批量训练所有模型）
训练所有模型.bat

# 或单个训练
conda activate AiDiagnosis-3.12
python train_resnet.py --task tongue_color --data_path ./data --epochs 50 --batch_size 32
python train_resnet.py --task tongue_coat_color --data_path ./data --epochs 50 --batch_size 32
python train_resnet.py --task thickness --data_path ./data --epochs 50 --batch_size 32
python train_resnet.py --task rot_and_greasy --data_path ./data --epochs 50 --batch_size 32
```

#### 选项B：批量训练（一次性训练所有模型）

```bash
# Windows
train_all.bat

# 或者手动编辑 train_all.bat 修改参数后运行
```

**训练完成后**：
- 最佳模型保存在：`.\checkpoints\{task}_best.pth`
- 例如：`.\checkpoints\tongue_color_best.pth`

### 步骤6：部署模型（将训练好的模型复制到项目目录）

```bash
# 部署YOLOv5模型
copy runs\train\tongue_detection\weights\best.pt application\net\weights\yolov5.pt

# 部署ResNet模型
copy .\checkpoints\tongue_color_best.pth application\net\weights\tongue_color.pth
copy .\checkpoints\tongue_coat_color_best.pth application\net\weights\tongue_coat_color.pth
copy .\checkpoints\thickness_best.pth application\net\weights\thickness.pth
copy .\checkpoints\rot_and_greasy_best.pth application\net\weights\rot_and_greasy.pth
```

### 步骤7：测试训练好的模型

```bash
# 重启应用
python run.py
```

然后在前端上传图片测试，看看模型效果如何。

---

## ⚙️ 训练参数建议

### YOLOv5参数
- **批次大小 (batch)**：16-32（根据GPU内存）
  - GPU内存8GB：建议16
  - GPU内存16GB+：可以32
- **训练轮数 (epochs)**：100-200
  - 数据量少（<500张）：100-150
  - 数据量多（>1000张）：150-200
- **图像尺寸 (img)**：640（标准，一般不改）

### ResNet参数
- **批次大小 (batch_size)**：32（GPU）或 16（CPU）
- **学习率 (lr)**：0.001（默认，一般不改）
- **训练轮数 (epochs)**：50-100
  - 数据量少：50
  - 数据量多：100

---

## ❓ 常见问题

### Q1: 我没有GPU，可以用CPU训练吗？
**A**: 可以，但速度会很慢。建议：
- YOLOv5：CPU训练可能需要几天时间
- ResNet：每个任务可能需要几小时到一天
- 强烈建议使用GPU训练

### Q2: 训练过程中断了怎么办？
**A**: 
- YOLOv5：使用 `--resume` 参数恢复训练
  ```bash
  python train_yolov5.py --data data\tongue.yaml --resume runs\train\tongue_detection\weights\last.pt
  ```
- ResNet：使用 `--resume` 参数恢复训练
  ```bash
  python train_resnet.py --task tongue_color --data_path ./data --resume .\checkpoints\tongue_color_epoch_50.pth
  ```

### Q3: 训练时内存不足怎么办？
**A**: 
- 减小批次大小（batch/batch_size）
- 减少训练图像尺寸
- 关闭其他占用内存的程序

### Q4: 如何判断模型训练好了？
**A**: 
- 验证准确率（Val Acc）不再提升
- 训练损失和验证损失都收敛
- 在测试集上表现良好
- 一般验证准确率达到80%+就可以考虑使用了

### Q5: 训练需要多长时间？
**A**: 
- YOLOv5（GPU）：几小时到一天
- ResNet每个任务（GPU）：1-3小时
- CPU训练：时间会显著增加

---

## 📚 详细文档

如果需要更详细的说明，请查看：
- **数据标注详细指南**：`DATA_ANNOTATION_GUIDE.md`
- **训练详细指南**：`TRAINING_GUIDE.md`
- **快速开始**：`QUICK_START_TRAINING.md`

---

## 🎉 完成！

训练完成后，你的模型就可以使用了！如果遇到问题，请查看错误日志或参考详细文档。





