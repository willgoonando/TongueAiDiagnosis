# 🎮 RTX 3050 (4GB) 训练完整指南

**显卡**: NVIDIA RTX 3050 (4GB 显存)
**CUDA**: 12.9（已检测到）
**目标**: 训练带注意力机制的 YOLOv5

---

## ✅ 当前状态

- ✅ **训练集标注**: 1177/1177 (100%)
- ✅ **验证集标注**: 295/295 (100%)
- ✅ **数据格式**: 已验证通过
- ⚠️ **PyTorch**: 当前是 CPU 版本，需要安装 CUDA 版本

---

## 🚀 第一步：安装 CUDA 版本的 PyTorch

### 方法1：使用批处理脚本（推荐）

```bash
安装CUDA版PyTorch.bat
```

### 方法2：手动安装

```bash
conda activate AiDiagnosis-3.12

# 卸载 CPU 版本
pip uninstall -y torch torchvision torchaudio

# 安装 CUDA 12.1 版本（你的 CUDA 是 12.9，兼容 12.1）
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
```

### 验证安装

```bash
conda activate AiDiagnosis-3.12
python -c "import torch; print('CUDA:', torch.cuda.is_available()); print('Device:', torch.cuda.get_device_name(0) if torch.cuda.is_available() else 'CPU')"
```

应该显示：
```
CUDA: True
Device: NVIDIA GeForce RTX 3050
```

---

## 🎯 第二步：训练配置（4GB 显存优化）

### 推荐参数

| 参数 | 值 | 说明 |
|------|-----|------|
| **Batch Size** | **8** | 4GB显存推荐8，如果OOM改为4 |
| **Image Size** | **640** | 标准尺寸 |
| **Epochs** | **100** | 总训练轮数 |
| **Device** | **0** | 使用第一块GPU |

### 如果显存不足（OOM错误）

**方案1：减小 Batch Size**
```batch
set BATCH=4  # 在批处理脚本中修改
```

**方案2：减小 Image Size**
```batch
set IMG_SIZE=512  # 从640改为512
```

**方案3：使用梯度累积**
- 保持 batch=4，但累积2次（等效batch=8）

---

## 📝 第三步：开始训练

### 训练策略（推荐）

**选项1：两阶段训练（推荐）**

1. **阶段1**：冻结 Backbone，只训练注意力模块（20 epochs）
2. **阶段2**：端到端微调（80 epochs）

**选项2：单阶段训练**

- 直接端到端训练（100 epochs）

### 开始训练

```bash
训练YOLOv5注意力模型.bat
```

脚本会：
1. ✅ 自动检测 CUDA
2. ✅ 使用 GPU 训练（device=0）
3. ✅ 使用优化的 batch size（8）
4. ✅ 训练完成后自动部署模型到 `application/net/weights/yolov5.pt`

---

## 📊 训练过程监控

### 查看训练进度

训练过程中会显示：
- 每个 epoch 的损失值
- mAP@0.5 和 mAP@0.5:0.95
- 训练和验证的损失曲线

### 查看 GPU 使用情况

打开另一个终端：
```bash
nvidia-smi -l 1
```

监控：
- GPU 利用率（应该接近 100%）
- 显存使用（应该在 3-4GB 左右）
- 温度（应该 < 80°C）

---

## 🎯 模型保存位置

### 训练输出

- **最佳模型**: `runs/train/tongue_cbam/weights/best.pt`
- **最后模型**: `runs/train/tongue_cbam/weights/last.pt`

### 自动部署

训练完成后，脚本会自动复制到：
- **项目模型**: `application/net/weights/yolov5.pt` ✅

**注意**: 
- ✅ 格式是 `.pt`（YOLOv5 标准格式）
- ✅ 不是 `.pth`
- ✅ 已自动部署到正确位置

---

## ⚠️ 常见问题

### 1. OOM（显存不足）错误

**错误信息**: `RuntimeError: CUDA out of memory`

**解决方案**:
1. 减小 batch size: 8 → 4 → 2
2. 减小 image size: 640 → 512
3. 关闭其他占用 GPU 的程序

### 2. CUDA 不可用

**检查**:
```bash
python -c "import torch; print(torch.cuda.is_available())"
```

**解决**: 运行 `安装CUDA版PyTorch.bat`

### 3. 训练速度慢

**可能原因**:
- 使用了 CPU 而不是 GPU
- Batch size 太小
- 数据加载线程数不够

**解决**:
- 确保使用 `--device 0`
- 增加 `--workers 4`

---

## 📈 预期训练时间

### RTX 3050 (4GB) 估算

- **阶段1**（20 epochs，batch=8）: 约 2-4 小时
- **阶段2**（80 epochs，batch=8）: 约 8-16 小时
- **总计**: 约 10-20 小时

### 优化建议

- 如果时间紧张，可以先训练 50 epochs 验证效果
- 如果效果好，再继续训练到 100 epochs

---

## 🚀 快速开始

### 1. 安装 CUDA 版 PyTorch

```bash
安装CUDA版PyTorch.bat
```

### 2. 验证 CUDA

```bash
conda activate AiDiagnosis-3.12
python -c "import torch; print('CUDA:', torch.cuda.is_available())"
```

### 3. 开始训练

```bash
训练YOLOv5注意力模型.bat
```

### 4. 等待训练完成

- 模型会自动保存到 `application/net/weights/yolov5.pt`
- 训练日志在 `runs/train/tongue_cbam/`

---

## ✅ 检查清单

训练前确认：
- [ ] CUDA 版本的 PyTorch 已安装
- [ ] CUDA 可用（`torch.cuda.is_available() == True`）
- [ ] 验证集已标注（295张）
- [ ] 数据格式检查通过
- [ ] 显存充足（关闭其他 GPU 程序）

---

准备好了吗？先运行 `安装CUDA版PyTorch.bat`，然后开始训练！



