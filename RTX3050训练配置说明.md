# 🎮 RTX 3050 (4GB) 训练配置说明

**显卡**: NVIDIA RTX 3050 (4GB 显存)
**目标**: 训练带注意力机制的 YOLOv5

---

## ⚠️ CUDA 环境检查

当前检测到 CUDA 不可用，需要安装 CUDA 版本的 PyTorch。

### 步骤1：安装 CUDA 版本的 PyTorch

**RTX 3050 支持 CUDA 11.8 或 12.1**

```bash
conda activate AiDiagnosis-3.12

# 卸载 CPU 版本的 PyTorch
pip uninstall torch torchvision torchaudio

# 安装 CUDA 11.8 版本的 PyTorch（推荐，兼容性好）
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118

# 或安装 CUDA 12.1 版本（如果 CUDA 12.1 已安装）
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
```

### 步骤2：验证 CUDA 安装

```bash
conda activate AiDiagnosis-3.12
python -c "import torch; print('CUDA available:', torch.cuda.is_available()); print('Device:', torch.cuda.get_device_name(0) if torch.cuda.is_available() else 'CPU')"
```

应该显示：
```
CUDA available: True
Device: NVIDIA GeForce RTX 3050
```

---

## 🎯 4GB 显存训练参数配置

### 推荐配置（4GB 显存）

| 参数 | 推荐值 | 说明 |
|------|--------|------|
| **Batch Size** | **8** | 4GB显存建议8，如果OOM可以降到4 |
| **Image Size** | **640** | 标准尺寸，可以降到512节省显存 |
| **Workers** | **4** | 数据加载线程数 |
| **Mixed Precision** | **True** | 使用FP16混合精度，节省显存 |

### 如果显存不足（OOM错误）

1. **减小 Batch Size**: 8 → 4 → 2
2. **减小 Image Size**: 640 → 512 → 416
3. **使用梯度累积**: batch=4, accumulate=2（等效batch=8）
4. **关闭数据增强**: 减少内存占用

---

## 🚀 训练脚本配置

### 训练参数建议

**阶段1：冻结 Backbone（20 epochs）**
- Batch Size: 8
- Image Size: 640
- 学习率: 0.001

**阶段2：端到端微调（80 epochs）**
- Batch Size: 8
- Image Size: 640
- 学习率: 0.0001

---

## 📝 模型保存位置

训练完成后，模型会保存在：
- **训练输出**: `runs/train/tongue_cbam/weights/best.pt`
- **部署位置**: `application/net/weights/yolov5.pt` ✅

**注意**: YOLOv5 使用 `.pt` 格式，不是 `.pth`

---

## ⚡ 快速开始

### 1. 安装 CUDA 版本的 PyTorch

```bash
conda activate AiDiagnosis-3.12
pip uninstall torch torchvision torchaudio
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
```

### 2. 验证 CUDA

```bash
python -c "import torch; print('CUDA:', torch.cuda.is_available())"
```

### 3. 开始训练

```bash
训练YOLOv5注意力模型.bat
```

---

## 🔧 如果遇到 OOM（显存不足）

### 方案1：减小 Batch Size

修改 `训练YOLOv5注意力模型.bat`：
```batch
set BATCH=4  # 从8改为4
```

### 方案2：减小 Image Size

修改训练脚本，添加 `--img 512` 参数

### 方案3：使用梯度累积

在训练脚本中添加梯度累积逻辑

---

准备好了吗？先安装 CUDA 版本的 PyTorch，然后开始训练！



